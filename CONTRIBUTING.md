# Contributing to AetherForge

1. Fork and branch from `main`.
2. Keep the WebSocket framing in `src/ws-lite.js` RFC6455-compatible.
3. Run `node src/server.js` and load `http://localhost:8787` before opening a PR.
4. Prefer small, reviewable diffs.
